---
kind: pipeline
type: kubernetes
name: default

steps:
- name: package
  image: golang:alpine
  commands:
  - apk update
  - apk add zip git
  - go test
  - mkdir -p testorepo/bin
  - go build -o testorepo/bin/saludeyshon
  - tar -czf xyz.tar.gz testorepo/*
  - zip -f testorepo-$(git describe --tags).zip testorepo/

- name: gitea_release
  image: plugins/gitea-release
  settings:
    api_key:
      from_secret: gitea_api_key
    base_url: http://gitea:3000
    checksum:
      - md5
      - sha1
    files:
    - testorepo*
  when:
    event: tag
